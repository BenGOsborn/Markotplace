package proxy

func GetContainer() {
	// This should get a container or it will start a new one and add it to the list if it exists
	// It will also get the right port for the user
}

func ProxyHandle() {

}
